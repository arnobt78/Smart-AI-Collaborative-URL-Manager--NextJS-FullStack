import { ApiStatusSoftNavLoading } from "@/components/ui/SoftNavLoading";

/** C7.3: Instant soft-nav shell while slim status prefetch runs. */
export default function ApiStatusLoading() {
  return <ApiStatusSoftNavLoading />;
}
