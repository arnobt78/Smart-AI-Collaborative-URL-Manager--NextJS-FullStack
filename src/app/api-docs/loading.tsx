import { ApiDocsSoftNavLoading } from "@/components/ui/SoftNavLoading";

/** C7.3: Instant soft-nav shell — auth RSC continues under this. */
export default function ApiDocsLoading() {
  return <ApiDocsSoftNavLoading />;
}
