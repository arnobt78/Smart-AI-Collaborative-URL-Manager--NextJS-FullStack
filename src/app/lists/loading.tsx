import { ListsRouteSkeleton } from "@/components/ui/RoutePageSkeleton";

/** C6.6: Instant soft-nav shell while auth-gated lists RSC resolves. */
export default function ListsLoading() {
  return <ListsRouteSkeleton />;
}
