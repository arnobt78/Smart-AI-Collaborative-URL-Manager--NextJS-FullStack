import { ListsSoftNavLoading } from "@/components/ui/SoftNavLoading";

/** C6.8: Instant soft-nav shell; skipped when destination RQ cache is warm. */
export default function ListsLoading() {
  return <ListsSoftNavLoading />;
}
